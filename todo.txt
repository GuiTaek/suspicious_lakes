- fix height (erl.)
- fix air block in structure (erl.)

- test if it generates on land correctly (erl.)
- test if the gap between void and lake is correct (erl.)

- change g (erl.)

- remove deprecated infrastructure (erl.)
- remove some json fields that are not used

- config entkoppeln (erl.)
- fix g implementation (not wanted anymore)

- test for performance (erl.)

! turn off rotation of lakes (erl.)

- fix block in small_lake_1.nbt (erl.)

- update fabric (erl.)
- implement teleport logic (erl.)
- remove distinction between minecraft chunkpos and my cunkpos (it was needed because of my problem to get tests to work) (erl.)
- make LakeDestinationFinder a class with methods that implicidly have a config (erl.)
- rename smallPrimeConfig (erl.)
- ConfigInstance shouldn't depend on WorldBorderConfigUpdater (erl.)
- is rotation turned on? (erl.)
- enderlakes->suspiciouslakes
- generate a single source to do the least destruction and convert air to endstone
- update at least to minecraft:1.21.1
